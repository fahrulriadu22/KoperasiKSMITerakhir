name: koperasi_ksmi
description: "Aplikasi Koperasi KSMI"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  dio: ^5.0.0
  image_picker: ^1.1.0
  http: ^1.1.0
  image: ^4.0.17  # ✅ Untuk image processing
  # ✅ UPDATE FIREBASE KE VERSI TERBARU
  firebase_core: ^4.2.0
  firebase_messaging: ^16.0.3
  flutter_local_notifications: ^19.0.0
  permission_handler: ^12.0.1
  cupertino_icons: ^1.0.8
  device_info_plus: ^12.0.0
  path_provider: ^2.1.5
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0  # ✅ UPDATE KE VERSI TERBARU
  flutter_launcher_icons: ^0.14.4  # ✅ UPDATE KE VERSI TERBARU

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/images/KSMI_LOGO.png"
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/images/KSMI_LOGO.png"

flutter:
  uses-material-design: true

  assets:
    - assets/images/KSMI_LOGO.png